# ~/.tmuxinator/l-k9s.yml

name: l-k9s
root: ~/workspace/doe-git/devops/tg/devops.tg.vault.mutating.webhook.live/ctx


windows:
  - port.forward:
      panes:
        - first:
            - kwai
  - local:
      panes:
        - first:
            - kwai

