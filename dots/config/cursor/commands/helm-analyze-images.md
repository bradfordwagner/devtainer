1. Find all images used in this helm chart

2. Create a table showing:
   - Image name
   - What it's used for
   - Fully qualified name (${registry}/$path:$tag format)

3. After the table, provide a comma-separated list of all fully qualified image names with no whitespaces around the commas

