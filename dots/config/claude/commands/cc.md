Run `git diff HEAD` and `git status --short` to see all staged and unstaged changes, then produce a single conventional commit message line. Format: `type(scope): description` — under 72 characters. Output the commit message line, then ask the user to approve it. If approved, run `git add -u && git commit -m "<message>" && git push`. Do not add a Co-Authored-By footer.
