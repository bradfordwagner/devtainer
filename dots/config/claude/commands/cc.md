Run `git diff HEAD` and `git status --short` to see all staged and unstaged changes. Based on the files you actually edited or created during this session (not merely read), identify only those relevant files. Produce a single conventional commit message line. Format: `type(scope): description` — under 72 characters. Output the commit message line and the list of session files you plan to stage, then ask the user to approve. If approved, run `git add <session files> && git commit -m "<message>" && git push`. Do not add a Co-Authored-By footer.
