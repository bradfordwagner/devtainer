Produce a concise summary of what we've accomplished so far in the current session. Base it only on the work actually done in this conversation — the tasks tackled, files created or edited, commands run, and decisions made. Do not include things merely discussed but not acted on unless they're open next steps.

Prefer tables — they're easier to digest. Format the output as:

- One line stating the **Goal** of the session.
- A **Done** table of concrete changes/outcomes with columns: `Change | File(s) | Notes` (reference files as `path:line` where useful).
- A **Next** table of remaining/follow-up items with columns: `Item | Status`, or the single line "Nothing outstanding." if complete.

Keep it tight — no preamble, no restating this instruction, just the summary. Do not run any tools or make changes; this is a read-only recap of the session.
